# Outputs

This folder is reserved for generated project artifacts such as cohort count tables, validation summaries, figures, and model outputs.

Generated files are ignored by default to avoid committing large artifacts or premature results. Small final tables or figures may be intentionally added later if they are appropriate for the portfolio and do not include real patient data.
